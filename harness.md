# AI ハーネスに関する理解の整理

AI エージェントを安全に、意図した範囲で、継続的に活用するためには、  
単にプロンプトを工夫するだけでは不十分である。  
AI は自律的に行動し、外部システムに影響を与える可能性があるため、  
その行動を **人間のガバナンスの中に収めるための仕組み** が必要になる。

この仕組みの総称が **「ハーネス（Harness）」** である。

ハーネスは 5 つの主要なレイヤーで構成されており、  
それぞれが異なる役割を持ちながら、積み上げ構造で AI の安全性を支えている。

---

## 1. 仕様ハーネス

仕様ハーネスとは、AI エージェントの **役割・権限・禁止事項・期待される振る舞い** を  
文章として明確に定義する枠組みである。

たとえば AGENTS.md や CLAUDE.md のような文書がこれに該当する。

AI は仕様を守ろうとするが、仕様に書かれていないことは守れない。  
そのため、仕様ハーネスは「言語的なガードレール」として機能するが、  
実際の制御は後述する実行ハーネスなどと組み合わせる必要がある。

個人開発レベルでは、この仕様ハーネスが最も中心的な役割を果たす。

---

## 2. テストハーネス

テストハーネスとは、AI が生成した成果物（特にコード）を  
**人間が定義したテスト基準で強制的に検証する仕組み**である。

GPT 5.5 や Claude Code のようなモデルは、自分でテストを書いたり実行したりするが、  
それはあくまで **AI の自己評価** に過ぎない。  
人間やプロジェクトが求める品質基準と一致しているとは限らない。

そのため、テストハーネスでは以下を行う：

- 人間（＋AI補助）がテスト仕様を定義する  
- テストケースを用意する  
- CI などで自動実行し、パスしない限り採用しない  

また、「開発する AI」と「テストする AI」を分ける構造も成立するが、  
テスト仕様そのものは人間が源泉である必要がある。

---

## 3. 実行ハーネス

実行ハーネスとは、AI が **外の世界に触れるときの行動範囲・権限・影響範囲** を  
技術的に制御する枠組みである。

ここでいう「外の世界」とは：

- 自分の PC の外（インターネット、外部 API、クラウド）
- 自分の PC 内の他のファイルや設定
- GitHub や本番環境などの外部リソース

Codex や Claude Code のサンドボックスは、  
AI の「内側の世界」を守るための安全装置であり、  
外側の世界を守ることはできない。

そのため、外側の実行ハーネスとして以下のような制御が必要になる：

- ネットワークアクセスの制限  
- API キーの権限最小化  
- 危険コマンドの禁止  
- ファイルアクセス範囲の限定  

例えとして「進撃の巨人のウォール構造」は非常に正確で、
AI のサンドボックスがウォール・シーナ、  
PC の制御がウォール・ローゼ、  
外部サービスの制御がウォール・マリアに相当する。

---

## 4. 監視ハーネス

監視ハーネスとは、AI が **外の世界で何をしたか** を  
AI 自身ではなく、外部の仕組みが **第三者視点で記録する枠組み**である。

これは情報セキュリティにおける **Audit（監査ログ）** の AI 版と考えられる。

Codex や Claude Code のログは AI の「内側の世界」のログであり、  
外側の世界での行動は記録できない。

監視ハーネスでは以下のようなログを扱う：

- ネットワーク通信ログ  
- コマンド実行ログ  
- ファイルアクセスログ  
- 外部サービス（GitHub など）の監査ログ  
- AI エージェントの行動ログ（意図の記録）  

これらを統合するのは大変だが、  
個人開発では最小構成（ネットワーク・コマンド・行動ログ）で十分機能する。

---

## 5. 運用ハーネス

運用ハーネスとは、AI エージェントが **本番で動き続けるための運用レイヤー**であり、  
「IT システムの運用管理」の AI 版と考えられる。

運用ハーネスは以下の 3 つの柱で構成される：

1. **通知（気づく）**  
   AI がやらかした瞬間に検知する  
   例：エラー検知、異常挙動の通知

2. **復旧（元に戻す）**  
   やらかした後に元に戻せる  
   例：Git のロールバック、設定ファイルのバックアップ

3. **予防（そもそもやらかさせない）**  
   本番とステージングの分離、権限最小化、二段階承認など

監視ハーネスが「記録」であるのに対し、  
運用ハーネスは「対応・復旧・予防」を担う。

---

## 6. ハーネスの境界と積み上げ構造

5 つのハーネスはそれぞれ役割が異なるが、  
完全に独立しているわけではなく、  
**下から上に積み上がる構造**になっている。

- 仕様ハーネスが AI の役割を定義し  
- テストハーネスが成果物を検証し  
- 実行ハーネスが行動範囲を制御し  
- 監視ハーネスが行動を記録し  
- 運用ハーネスが事故対応と予防を行う  

この積み上げ構造を理解することで、  
「どの問題をどのハーネスで解決すべきか」が明確になる。

---

## 7. 個人開発としてどこまで整備すべきか

監視ハーネスや運用ハーネスを網羅的に整備するのは、  
企業レベルでも大変な作業である。

個人開発では、以下の最小構成で十分に強い：

- 仕様ハーネス（AGENTS.md など）  
- テストハーネス（CI で自動テスト）  
- 実行ハーネス（権限最小化・危険操作の制限）  
- 監視ハーネス（ネットワーク・コマンド・行動ログの最小構成）  
- 運用ハーネス（Git ロールバック、PR 経由の変更）  

これにより、AI エージェントの暴走や事故を大幅に防ぐことができる。

---

## 8. この文書の位置づけ

本ドキュメントは、2026 年時点の**AI ハーネスの全体像と解釈の記録**である。

特に以下の点を自分の言葉で整理したものになっている：

- 内側と外側の安全の違い  
- 5 つのハーネスの役割と境界  
- 個人開発としてどこまで整備すべきか  
- ハーネス全体の積み上げ構造  

今後、AI エージェントを本格運用していく中で、  
どのハーネスが足りていなかったか、どこを強化すべきだったかを  
振り返るための基準点として活用できる。

